for i in range(3):
    img = cv2.imread(os.path.join(gtFine_path, 'train', random_city, list_files_gtFine[random_index*4 + i]))

    na = np.array(img)
    colours, counts = np.unique(na.reshape(-1,3), axis=0, return_counts=1)

    print(f"{substring_after(list_files_gtFine[random_index*4 + i], 'gtFine_')} :")

    for j in range(len(colours)):
        print(f"    {colours[j]}: {counts[j]}")

``random_city`` is clearly a function that got and finds a random city name, i can modify it to have only one city, in my case aachen.
``list_files_gtFine``is another functions that find the files related to the city.
